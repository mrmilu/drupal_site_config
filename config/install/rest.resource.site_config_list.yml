langcode: es
status: true
dependencies:
  module:
    - serialization
    - site_config
    - user
id: site_config_list
plugin_id: site_config_list
granularity: resource
configuration:
  methods:
    - GET
  formats:
    - json
  authentication:
    - cookie
